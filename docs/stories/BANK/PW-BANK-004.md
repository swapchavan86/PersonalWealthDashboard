# PW-BANK-004 — Implement transaction categorization and normalization

## Goal
Apply deterministic banking business rules to canonical transaction data.

## Acceptance Criteria
- Normalization and categorization are separate from document parsing.
- Rules are deterministic and unit-testable.
- Categories are not hard-coded into UI code.
- Manual overrides can be represented without destroying source data.

## Status
Ready
